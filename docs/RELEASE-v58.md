# v58 Humanized SuperAdmin UI
The raw JSON Add/Edit experience has been removed. SuperAdmin now has tenant context selection and can create tenant-owned data on behalf of a selected tenant. TenantId is context rather than a manually typed form field. Platform Management uses a tenant dropdown. Add/Edit/Delete remain contextual page/table actions; the sidebar remains navigation.
