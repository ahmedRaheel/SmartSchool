namespace SmartSchool.Modules.Finance.Contracts;

public sealed record InvoiceResponse(
    Guid Id,
    string Code,
    string Name,
    bool IsActive,
    string? MetadataJson,
    DateTimeOffset CreatedAt,
    DateTimeOffset? UpdatedAt)
{
    public static InvoiceResponse FromEntity(
        Models.Invoice entity)
    {
        return new InvoiceResponse(
            entity.Id,
            entity.Code,
            entity.Name,
            entity.IsActive,
            entity.MetadataJson,
            entity.CreatedAt,
            entity.UpdatedAt);
    }
}
