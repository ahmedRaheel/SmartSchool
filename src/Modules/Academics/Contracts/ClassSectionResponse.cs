namespace SmartSchool.Modules.Academics.Contracts;

public sealed record ClassSectionResponse(
    Guid Id,
    string Code,
    string Name,
    bool IsActive,
    string? MetadataJson,
    DateTimeOffset CreatedAt,
    DateTimeOffset? UpdatedAt)
{
    public static ClassSectionResponse FromEntity(
        Models.ClassSection entity)
    {
        return new ClassSectionResponse(
            entity.Id,
            entity.Code,
            entity.Name,
            entity.IsActive,
            entity.MetadataJson,
            entity.CreatedAt,
            entity.UpdatedAt);
    }
}
